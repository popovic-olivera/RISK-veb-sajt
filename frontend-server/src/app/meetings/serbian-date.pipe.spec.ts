import { SerbianDatePipe } from './serbian-date.pipe';

describe('SerbianDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SerbianDatePipe();
    expect(pipe).toBeTruthy();
  });
});
