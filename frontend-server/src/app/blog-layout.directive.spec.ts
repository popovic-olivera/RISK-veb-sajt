import { BlogLayoutDirective } from './blog-layout.directive';

describe('BlogLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
